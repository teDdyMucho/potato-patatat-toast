# EOD Report — AKT Virtual Assistance Services
**Date:** May 29, 2026
**Session:** Full-stack update — mobile responsiveness, UI/UX, SEO/GEO/AEO, backend, new features

---

## 1. MOBILE RESPONSIVENESS

### Overview (Home) Page
- Fixed `100vh` → `100dvh` so bottom CTA/partner strip stays visible behind mobile browser chrome
- Added `pb-[env(safe-area-inset-bottom)]` for iPhone notch/home indicator
- Nav breakpoints shifted from `md` (768px) to `lg` (1024px) — tablets now use hamburger layout, not broken tab overflow

### Navigation (all pages)
- **Hamburger menu** fully redesigned: icon-pill rows per link, active page highlighted with teal border + glowing dot, Admin/Login/Logout with icons, green "Book a Consultation" CTA at the bottom
- **Desktop dropdown** (user menu): added "Manage Account" link above Log out
- **Mobile menu**: added "Manage Account" link above Log out
- **Current page title** shown centered in header on non-home pages (mobile/tablet only)
- **GHL header image** scoped to homepage only (no other pages)

### GHL (GoHighLevel) Badge
- Desktop: floating right-side animated badge (unchanged)
- Mobile header: GHL image centered in nav with pulsing green glow animation (homepage only)
- Body: animated "Try GoHighLevel now" + curly arrow below the header image (mobile/tablet only)
- GHL image color: dark teal tint applied via CSS filter + mix-blend overlay to match site palette

### About Page
- 3D AKT logo hidden on mobile/tablet (prevents WebGL load on phones)
- Tech stack section → continuous **auto-scrolling marquee carousel** on mobile/tablet, wrapped grid on desktop
- Process section → 2-column layout: steps (left) + **founder photo card** (right), vertically centered

### AI Tools Page
- Category filter chips → **swipeable horizontal scroll row** on mobile (no more ragged wrap)

### Contact Page
- "Full Name" split into **First Name + Last Name** separate fields
- Removed "Preferred Contact Time" field
- "What Do You Need?" → **multi-select chips** with animation (green dot + check icon on select, live "Selected: X · Y" display)
- "Send Message" button → enhanced with gradient, shimmer sweep on hover, loading spinner

### Login Page
- Marketing panel (3D logo + "Why your business needs AKT") hidden on mobile/tablet

---

## 2. NEW FEATURES

### Booking Modal (Contact Page)
- After successful form submission → booking modal opens automatically
- Embeds `https://api.leadconnectorhq.com/widget/booking/rBZBds2Y2BtTaXMDeEk7` as a full iframe
- **Auto-fills** from contact form: `first_name`, `last_name`, `email`, `phone` via URL query params
- Centered modal, responsive on all screen sizes, animated in/out

### Account Management Page (`/account`)
- New page accessible from user dropdown → "Manage Account"
- **Display Name**: editable, calls `supabase.auth.updateUser()`, shows "Saved!" confirmation
- **Change Password**: re-authenticates with current password first, then updates, shows success
- Email shown as read-only
- Redirects to login if not authenticated

### Blog Post Content Renderer
- `# Heading` → bold display heading (Space Grotesk, size by `#` level)
- `* bullet` → teal glowing dot bullet list
- `---` → green horizontal divider line
- Mixed blocks supported

### Blog Editor Auto-Draft
- Form data auto-saved to `localStorage` every 800ms while typing
- On re-open (Alt+Tab, close modal, refresh) → data is restored + "Draft restored" banner shown
- "Discard draft" button to reset to last saved state
- Draft cleared on successful save

### Business FB Ads Tool
- Added new AI tool card to `/ai-tools`
- Category: **Marketing AI** (new filter chip added to scroll row)
- Generates Facebook/Instagram ad copy (headlines, primary text, CTAs)

### Founder Photo Card (About — Process Section)
- Added founder photo (`bossgelomapagmahal.png`) to the right of the process steps
- Same HUD card style: teal corner brackets, sweep animation, pulsing glow, scroll-triggered reveal
- Name badge overlay: "Jose Angelo Tapang / CEO & Founder"
- Vertically centered against all 5 process steps

---

## 3. SEO / GEO / AEO

### New Infrastructure Files
| File | Purpose |
|------|---------|
| `app/robots.ts` | Explicitly allows GPTBot, ClaudeBot, PerplexityBot, Google-Extended, FacebookBot |
| `app/sitemap.ts` | Dynamic XML sitemap — all static routes + 10 partner slugs + live blog posts from Supabase |
| `public/llms.txt` | AI guidance file — tells LLMs what AKT is, what to cite, key pages, entity info |

### Metadata & Structured Data
- `app/services/layout.tsx` — Services metadata + `Service` + `OfferCatalog` JSON-LD
- `app/contact/layout.tsx` — Contact metadata + `ContactPage` JSON-LD
- `app/ai-tools/layout.tsx` — AI Tools metadata + `WebApplication` JSON-LD
- `app/login/layout.tsx` — Login metadata (`noindex`)
- `app/page.tsx` — `Organization` + `WebSite` + `SearchAction` JSON-LD (homepage)
- `app/blog/[slug]/page.tsx` — `Article` JSON-LD + canonical URL + Twitter card per post
- `app/partners/page.tsx` — Upgraded metadata with keywords, OG, canonical

---

## 4. BACKEND / DATABASE

### Leads API (`/api/leads/route.ts`)
- Accepts `firstName` + `lastName` separately (not combined `name`)
- Saves to Supabase via `save_lead()` PostgreSQL function (RPC)
- Falls back to direct insert if function not yet deployed
- **Fires GHL webhook** non-blocking: `https://primary-production-6722.up.railway.app/webhook/ghl-savecontact-aktservices-bookconsultation`
- Webhook payload includes: `lead_id`, `user_id`, `first_name`, `last_name`, `name`, `email`, `phone`, `company`, `need`, `message`, `source`, `site`, `submitted_at`

### Supabase SQL (`docs/leads-table.sql`)
- `public.leads` table with `first_name`, `last_name` columns (safe `ALTER TABLE ADD COLUMN IF NOT EXISTS`)
- `save_lead()` PostgreSQL function — `SECURITY DEFINER`, accepts all form fields
- Auto-update trigger on `updated_at`
- Indexes on `email`, `status`, `created_at`, `user_id`
- RLS policies

### Blog
- **Removed all mock/fallback data** — blog now shows only real Supabase posts
- Empty state shown when no posts are published yet
- Mock slugs return proper 404

---

## 5. DESIGN / BRANDING

### Font — Space Grotesk
- Replaced `Syne` with **Space Grotesk** for all headings site-wide
- Aliased via Tailwind `font-syne` utility so zero per-file changes needed

### Heading Shimmer Animation
- `.heading-shimmer` CSS class — teal→white→teal gradient sweep on a 5s loop
- Applied to About page hero heading

### Favicon
- Replaced default Next.js favicon with **AKT brain logo** (`app/icon.png`)
- Old `favicon.ico` removed so browser uses `icon.png`

### 3D Logo — Auto Mouse Follow
- Removed drag interaction entirely
- Logo now **automatically tracks the mouse/touch position** globally
- Smooth LERP interpolation (factor 0.06) — elastic lag for natural feel
- Touch support for mobile finger tracking
- Applied to all 3 pages using `Akt3DLogo`: Overview, About (desktop), Login (desktop)

---

## 6. PENDING / NEXT STEPS

- [ ] Run `docs/leads-table.sql` in Supabase SQL Editor (adds `first_name`, `last_name` columns + `save_lead()` function)
- [ ] Configure Google OAuth in Supabase (Authentication → Providers → Google) + Google Cloud Console
- [ ] Add Vercel redirect URL in Supabase: `https://aktservices.org/auth/callback`
- [ ] Submit sitemap in Google Search Console: `https://aktservices.org/sitemap.xml` ✅ (done)
- [ ] Wire up "Business FB Ads" AI tool with actual Claude API route
- [ ] Continue mobile responsive pass: Services, Partners, Blog, Admin pages
- [ ] Publish real blog posts via `/admin/content/blog`

---

*Generated: May 29, 2026 — AKT Virtual Assistance Services*
